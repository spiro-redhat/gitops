apiVersion: tekton.dev/v1alpha1
kind: Pipeline
metadata:
  name: tutorial-pipeline
  annotations:
    argocd.argoproj.io/sync-wave: "0"
spec:
  tasks:
    - name: hello-world
      taskRef:
        name: echo
      params:
        - name: TERM
          value: "Hello world"
        